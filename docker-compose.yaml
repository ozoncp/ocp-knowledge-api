version: '3.7'

services:

  postgres:
    image: postgres:latest
    ports:
    - "5432:5432"
    environment:
      POSTGRES_DB: knowledge_db
      POSTGRES_PASSWORD: admin
      POSTGRES_USER: admin
